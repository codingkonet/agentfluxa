{
  "name": "agentfluxa",
  "version": "1.0.0",
  "private": true,
  "description": "AgentFLUXA - unified chat UI for OpenAI, Gemini, Copilot and OpenRouter models",
  "workspaces": [
    "server",
    "client",
    "cli"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "build:client": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "chat": "node cli/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
